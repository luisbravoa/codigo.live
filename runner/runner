file="$1"
# run  --read-only -v `pwd`:`pwd` -w `pwd` -i -t  runner node file.js

runner="docker run runner"

file=$1

ext"${file##*.}"
echo "$ext"

case "$ext" in
  "rb")
    ruby $file
    ;;
  *)
    echo "Not supported"
    exit 1
    ;;
esac
